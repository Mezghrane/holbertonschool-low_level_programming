#include "main.h"
#include <stdio.h>

/**
 * mul: multiplie two integers and returns the result
 * @a: The first integer to multiplie
 * @b: The second integer to multiplie
 *
 */
int mul(int a, int b)

{
	return (a * b);
}
